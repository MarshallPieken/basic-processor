# Basic Processor

This is to diagram a microprocessor, starting from digital circuitry all the way up to processor operations. (e.g., circuits -> logic gates -> multiplexors, bitwise operations, algorithmic operations, storage [latches &amp; flip-flops] -> VHDL/ISAs -> registers &amp; buffers -> instruction ordering &amp; logic units [ALUs, FPUs, etc.] -> microprocessor

## Things to Enumerate & Understand
**1. Logic gate circuitry & combinations**

- Make a writeup which details each logic gate in the form of circuitry to be able to explain each one. Include their truth tables.

- Also include combinations of logic gates to create other ones to demonstrate competence there.

**2. Multiplexors & Storage**
  
- Make another writeup which diagrams how logic gates are used in multiplexing, storage (latches & flip-flops).

**3. Bitwise Operations** 
- Make another write-up which displays how logic gates are used to execute arithmetic operations (e.g., adders, multiplication, division(? ew))

**4. Logic Units**

- Show how they can be used to create ALUs with multiplexing for decisions.
- ALSO include in here hoe those are written in VHDL for introduction to the next write-up

**5. Buffers, Decision-Making**

- Make a writeup for instruction ordering & dispatching (frontend <-> backend), buffer scheduling
